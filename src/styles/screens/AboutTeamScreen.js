import { StyleSheet } from 'react-native'

export default () => StyleSheet.create({
    containerImageUser: {
        width: 100,
        height: 100,
        borderRadius: 100,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    },
    imageUser: {
        width: 90,
        height: 90,
        borderRadius: 100,
    }
})